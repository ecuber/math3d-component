{
  "name": "math3d-component",
  "version": "1.0.0",
  "description": "Embedded math3d-react React component.",
  "engines": {
    "node": "16.1.x"
  },
  "scripts": {
    "start": "yarn --cwd ./server start & yarn --cwd ./app start",
    "update": "yarn --cwd ./app add math3d-component",
    "build:app": "yarn --cwd ./app build"
  },
  "cacheDirectories": [
    "server/node_modules",
    "client/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mars/heroku-cra-node.git"
  },
  "dependencies": {
    "yarn": "^1.22.11"
  }
}